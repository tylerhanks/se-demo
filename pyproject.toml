[project]
name = "se-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tylerhanks", email = "teh.hanks@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
se-demo = "se_demo:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
